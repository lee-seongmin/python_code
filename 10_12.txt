필드-인스턴스변수
메소드-동작

정적(멤버 변수)==> 메소드 바깥에서 선언하는 변수 객체를 처음 만들 때 하나만 생성된다.
정적변수에 접근하기 위해서는 클래스이름.정적변ㅅ

인스턴스변수

쓰지 않는 메모리를 반환==>소멸자__del__(self,...)

창구를 통해 접근자와 설정자

접근자 메소드와 설정자 메소드 2개가 필요하다.

스트링메소드는 객체에 대한 정보를 제공해준다.

<특수메소드> 찾아보기.

4가지 변수를 구분할 수 있어야 한다.

