파이썬 10 07
요약
list append(value), insert(index,value), extend
요소 삭제 키워드 : del[index] 메소드 : remove()
tuple
리스트로 변경 append()
dictionary
삭제 : pop()

8장 클래스와 객체
c++, c#, java
객체 지향 프로그래밍(OOP)

클래스 = 실체가 없다. ex ) 설계도 와플기계 ==> 객체 = ex) 와플, 컴퓨터
객체는 서로 다르게 운용할 수 있다.
인스텐시에이션 ==> 인스턴스==객체

객체는 "상태"와 "동작"을 가지고 있다.
객체의 상태는 객체의 "속성"이다.
객체의 동작은 객체가 취할 수 있는 동작(기능)이다.
상태는 인스턴스 변수로 표현
상태=> 순차회로 
상태정보는 변수 동작은 (함수 == 메소드)
객체 = 필드(변수전체)와 메소드(함수)
self = 메소드의 첫번째 argument라고 써야한다. why?알아보기

"캡슐화"
example) counter class작성

"생성자"
객체가 생성될 때 객체를 기본값으로 만드는 특수 메소드
__init__(self, ...): 생성자는 이 객체가 생성될 때 초기화작업을 해주는 것이다.
__메소드__ ==> 2개로 쓰인 메소드는 특수한 역할을 한다.

"첫번째 매개변수는 항상 self여야한다." 
self가 붙은 변수를 인스턴스 변수라고 한다.

"정보 은닉"

내부 변수값을 외부에서 설정하지 못하게 한다.

"변수는 안에 감추고 외부에서는 메소드 들만 사용하도록 한다."
__2개를 변수앞에 붙이면 외부에서 바꾸지 못하도록 한다.
대부분의 인스턴스 변수는 _2개를 넣는다.
__ : private변수 , _이 없는 것은 public으로 한다.

앞으로는 _2개를 사용해서 private로 사용해야 한다.

"접근자와 설정자"

내부 인스턴스 변수값을 읽어오는 것 ==> 접근자
내부 인스턴스 변수값을 설정하는 것 ==> 설정자
argument를 설정할 때 (name=NONE)으로 쓰면 만약 name이 입력되지 않았을 때 default값으로 none을 출력한다.

정리해놓으면 수업 끝나고 설명해줄게 
lab :원을 클래스로 표현

__str__==> return '(%d, %d, %d)' % (self.변수1, self.변수2, self.변수3)
객체를 부르면 객체에 대한 정보(변수1, 변수2, 변수3)를 넘겨준다.